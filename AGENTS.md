# Project Instructions

- Do not run lint unless explicitly requested by the user.
- For web deployment, use `./build_web.sh && ./restart_web.sh` from the project root.

## Support Ticket Replies

Never put the user's name in a ticket reply. Also never quote their wallet names,
amounts, balances, or any other account detail we looked up while investigating.
They must not learn how much we can see about their account. Describe the fix with
placeholders instead of their real values.
